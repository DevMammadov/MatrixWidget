export enum ESteps {
  step = 1,
  inline,
  menu,
}

export enum EMenuNavigate {
  ChooseEmployee = 0,
  ChooseServices = 1,
  ChooseDateTime = 2,
}

export enum EWidgetButtonPosition {
  BottomLeft = 1,
  BottomRight = 2,
  TopRight = 3,
  TopLeft = 4,
}

export enum EWidgetMode {
  prod = 1,
  develop,
}

export enum EWidgetPosition {
  left = 0,
  right,
}

export enum EAnimationType {
  fixed = 0,
  animated,
}
