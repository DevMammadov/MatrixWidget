export enum ESteps {
  step = 1,
  inline,
  menu,
}

export enum EMenuNavigate {
  ChooseEmployee = 0,
  ChooseServices = 1,
  ChooseDateTime = 2,
}

export enum EMenuSteps {
  MenuStep = -1,
  WorkerWithTime = 0,
  Services = 1,
  Time = 2,
  GeneralTime = 3,
  Contacts = 4,
}
